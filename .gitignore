# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# ZK assets synced from managed/ into public/ at build time (predev / prebuild).
# The source of truth is the committed managed/ directory; these are generated.
public/keys/
public/zkir/

# secrets — NEVER commit a wallet seed or funded key
.env
.env.*
!.env.example
*.seed
wallet-*.json

# logs & os cruft
*.log
.DS_Store
npm-debug.log*

# editor / AI-tooling local config (never publish)
.claude/
.vscode/
.idea/
